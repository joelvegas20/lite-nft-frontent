/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.gamma.io",
      "gaia.hiro.so",
      "images.unsplash.com",
      'encrypted-tbn0.gstatic.com',
    ],
  },
};

module.exports = nextConfig;
